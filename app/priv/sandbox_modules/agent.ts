// Micelio sandbox agent entry point
// This module is served by the Micelio server and fetched by Deno at runtime.
// It implements the ACP JSON-RPC 2.0 protocol over stdio.
//
// Usage: deno run --allow-all --reload https://<host>/sandbox/modules/agent.ts

console.error("[micelio-agent] Starting sandbox agent...");

// TODO: Implement ACP JSON-RPC 2.0 over stdio
// This is a placeholder that will be replaced with the full agent implementation.
